{
  "name": "agrilink-lop",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @lop/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
